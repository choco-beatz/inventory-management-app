import 'package:flutter/material.dart';

class CustomerLedger extends StatelessWidget {
  const CustomerLedger({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is customer"),
    );
  }
}